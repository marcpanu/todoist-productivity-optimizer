const express = require('express');
const router = express.Router();

// Placeholder for Gmail routes
router.get('/messages', async (req, res) => {
    res.json({ message: 'Gmail integration coming soon' });
});

module.exports = router;
